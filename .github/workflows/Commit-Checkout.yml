name: commit_checkout
on: [push]
jobs:
  external-workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: abystoma/external-workflow
      - name: Greetings
        run: echo "$(<greeting.txt)"
